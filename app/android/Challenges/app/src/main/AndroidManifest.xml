<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="challenges.com.challenges">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.CadastroActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.CadastroCriancaActivity" />
        <activity android:name=".activities.AntigaHome" />
        <activity android:name=".activities.HomeResponsavelActivity" />
        <activity android:name=".activities.ConfiguracoesResponsavelActivity" />
        <activity android:name=".activities.AdicionarDesafioActivity" />
        <activity android:name=".activities.AdicionarCriancaActivity" />
        <activity
            android:name=".activities.HomeCriancaActivity"
            android:label="@string/title_activity_main2" />
        <activity android:name=".activities.AdicionarAmigoActivity" />
    </application>

</manifest>